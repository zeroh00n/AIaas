
function Refuse(){
 /**  const renderCount = useRef(0);
    const [count, setCount] = useState(0);//useRef, useState는 이전 값을 기억해 계속 오른다 둘의 차이점은 렌더링을 피하고 싶으면 useRef를 사용하란것이다
    let cnt = 0; //cnt는 기억 이전 값을 기억 못해 계속 1

    const handleClick = ()=>{
        renderCount.current +=1;
        setCount(c=>c+1);
        cnt++;
        console.log(renderCount.current, cnt);
    } */ 
    return( 
       <>
        <div>
            <h1>hello world</h1>
        </div>
        </>  
    )
}
export default Refuse;