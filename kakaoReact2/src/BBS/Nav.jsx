import {Link, Outlet} from "react-router-dom";

const Navs = [{

}]

function Nav(){
    return(
        <div>
            <header></header>
            <Outlet />
        </div>
    )
}
export default Nav